---
title: 环境搭建指南
description: 如何配置本地AI创作环境，以及每一步的技术选择所对应的观念考量
---

# 环境搭建指南

本指南将帮助你在个人电脑上搭建一个极简的、本地运行的AI创作环境。每一步都附有**观念考量**，解释为什么选择这个工具/配置。

## 目标配置

- **模型运行框架**: Ollama
- **模型**: Llama 3.2 (3B参数版) 或其他轻量级模型
- **硬件要求**: 
  - 最低: 8GB RAM, 任意CPU
  - 推荐: 16GB RAM, 8GB+ VRAM的GPU
- **操作系统**: macOS / Linux / Windows

## 步骤1: 安装 Ollama

### macOS
```bash
brew install ollama
```

### Linux
```bash
curl -fsSL https://ollama.com/install.sh | sh
```

### Windows
下载安装包: https://ollama.com/download

### 💡 观念考量

**为什么选择 Ollama？**
- **极简**: 一条命令安装，无需复杂配置
- **本地**: 所有运算在本地完成，数据不上传
- **开源**: 基于开源项目，可审计
- **轻量**: 专为消费级硬件优化

**对比云端服务 (ChatGPT/Claude)**:
| 维度 | 云端 | Ollama本地 |
|------|------|-----------|
| 隐私 | 数据上传 | 完全本地 |
| 控制 | 黑箱 | 可配置 |
| 依赖 | 需要网络 | 离线可用 |
| 成本 | 订阅费 | 电费 |
| 速度 | 快 | 较慢（但可感知）|

## 步骤2: 下载模型

```bash
# 推荐：Llama 3.2 (3B参数，轻量但能力强)
ollama pull llama3.2:3b

# 备选：Mistral (7B参数，质量更高但需要更多资源)
ollama pull mistral:7b

# 备选：Phi-3 (Microsoft的小模型，3.8B参数)
ollama pull phi3:3.8b
```

### 💡 观念考量

**为什么选择"小模型"？**

1. **物质性可见**: 小模型在本地运行时，你能**感知到**硬件的限制——风扇转动、响应延迟。这种"笨拙"是创作体验的一部分。

2. **约束即创作**: 小模型的"能力不足"迫使你调整提示策略，这种调整本身就是创作。

3. **去中心化**: 不依赖科技巨头的API，维护创作的独立性。

**模型选择建议**:
- **初学者**: llama3.2:3b（平衡性能与资源）
- **追求质量**: mistral:7b（需要8GB+ VRAM）
- **极简主义**: phi3:3.8b（最小资源占用）

## 步骤3: 测试运行

```bash
# 启动交互式会话
ollama run llama3.2:3b
```

你会看到类似：
```
>>> 你好
你好！有什么我可以帮助你的吗？
```

输入 `/bye` 退出。

### 💡 观念考量

**命令行界面的价值**

与ChatGPT的图形界面相比，命令行：
- **更慢**: 没有自动补全，每次输入都是完整的思考
- **更可见**: 历史记录保存在本地文件中
- **更笨拙**: 这种"不顺畅"迫使你更审慎地表达

## 步骤4: 配置日志记录（可选但推荐）

创建一个脚本 `chat.sh`：

```bash
#!/bin/bash
TIMESTAMP=$(date +%Y%m%d_%H%M%S)
LOGFILE="logs/session_$TIMESTAMP.log"

mkdir -p logs
echo "=== Session started at $(date) ===" > $LOGFILE
echo "Model: llama3.2:3b" >> $LOGFILE
echo "================================" >> $LOGFILE

ollama run llama3.2:3b | tee -a $LOGFILE
```

赋予执行权限：
```bash
chmod +x chat.sh
```

### 💡 观念考量

**为什么记录日志？**

日志是[过程显形](../theory/process-visibilization.md)的基础材料。它记录了：
- 时间流逝（创作不是瞬时的）
- 决策序列（你如何调整提示）
- 技术痕迹（延迟、错误、系统状态）

## 下一步

环境搭建完成后，你可以：

1. **阅读 [约束设计模式](constraint-patterns.md)** — 学习具体的约束策略
2. **安装 constraint-cli** — 使用我们的研究工具
3. **开始第一次实践** — 尝试用约束条件与AI对话

## 故障排除

### 模型下载慢
```bash
# 设置镜像（中国用户）
export OLLAMA_HOST=0.0.0.0
# 或使用代理
```

### 内存不足
```bash
# 使用更小的模型
ollama pull llama3.2:1b
```

### GPU未识别
```bash
# 检查CUDA/Metal支持
ollama --version
# 查看日志
ollama serve 2>&1 | grep -i gpu
```

## 延伸阅读

- [本地性](../theory/locality.md) — 本地运算的理论基础
- [小模型](../theory/small-models.md) — 轻量级模型的美学意涵
- Ollama 官方文档: https://github.com/ollama/ollama
